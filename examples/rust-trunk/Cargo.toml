[package]
name = "symengine-rust-trunk"
version = "0.1.0"
edition = "2021"
description = "SymEngine + Rust + Trunk (wasm32-unknown-unknown) demo"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = "0.2"

[build-dependencies]
cc = "1"

[profile.release]
lto = true
opt-level = "s"
